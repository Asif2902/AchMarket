{
  "name": "achmarket",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "compile": "npx hardhat compile",
    "deploy": "npx hardhat run scripts/deploy.js --network arc_testnet",
    "upgrade:router": "npx hardhat run scripts/upgradeRouter.js --network arc_testnet",
    "deploy:hybrid:bounded": "npx hardhat run scripts/deployBoundedHybrid.js --network arc_testnet",
    "deploy:local": "npx hardhat run scripts/deploy.js --network hardhat",
    "clean": "npx hardhat clean",
    "size": "npx hardhat size-contracts"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox-mocha-ethers": "^3.0.5",
    "hardhat": "^3.4.5"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.1.0",
    "dotenv": "^17.3.1"
  }
}
